# E2E session review, run 2, 2026-09-10

Scope: audit of agent session `a2d330722396e15bb` (05:41Z to 06:12Z, 31 minutes, 64 Bash and 7 Read calls, zero tool errors). The agent built `FPS Drill` in `/Users/minjunes/blitz-games/fps-drill` from the 22:40 tarballs (monorepo `main` after fix pass F, commit `71483c7`), published it, and reported five notes. This review verifies the session, re-checks F1 to F17 from `docs/e2e-session-review-2026-09-10.md`, and checks the live game. Nothing was fixed or killed. No deploy token, claim secret, or editor session token appears in this file; `?t=<REDACTED>` marks the editor URL.

Sources: the transcript, the project folder, the monorepo sources (the tarballs are byte-identical to `packages/engine/dist/runtime.js`, sha `4c904890...`), `/Users/minjunes/blitz-cloud`, the backend API (deploy token read from `.blitz/deploys.json` in memory), and three headless Playwright loads (live page, editor load, editor Play and Stop).

## Summary

1. Fix pass F held: 11 of 17 findings are fixed and 5 of those were exercised by the agent (F1, F2, F3, F4, F10, F14); the game published in one try and plays live with zero console errors.
2. The runtime registry is still stale under `0.12.0` (registered `4c2d97fb...`, shipped `4c904890...`); every publish prints a warning and a strict backend would return 409 (G4).
3. Two feedback gaps remain: the editor registers every `.script.js` in the folder while the runtime registers only `blitz.scripts` (G1), and `.blitz/console.log` receives `console.error` only, never `console.warn` (G2, F9 partly fixed).
4. The session was 26 minutes faster than run 1 because the agent was not fresh: it read three memory notes and both earlier projects, and reused five `lib/` files and the scene tool from `fps-shooter`.
5. Paper cuts: `pull` before the first publish exits 1 (G6), `/api/*` refuses the URL token (G5), publish excludes are opt-in (G7), a missing-file GET on the dev server is a 500 (G3), and `state.json` goes stale when the tab closes mid-play (G8).

## 1. New findings

Line numbers point at the monorepo at `main` (`packages/...`) or at `/Users/minjunes/blitz-cloud`. Transcript lines are JSONL line numbers.

| id | severity | area | what happened | evidence | repro | root cause | suggested fix |
|---|---|---|---|---|---|---|---|
| G1 | major | editor + runtime | The editor registers every `*.script.js` and `*.plugin.js` in the project, including `samples/`. The published runtime registers only `blitz.scripts`. A component that is not listed works in the editor and is missing live. Nothing reports the difference. | Transcript 218: `PASS component types registered :: DrillManager, Generator, PlayerRig, Spin, TargetField` while `package.json` lists three scripts. `samples/Spin.script.js` was excluded from publish. Agent report: "The editor also lists `Spin` from `samples/`". | Write `scripts/Foo.script.js`, attach `Foo` to a node, leave `blitz.scripts` unchanged. Play in the editor: works. Publish: live console prints `EntityComponentPlugin: unknown component type Foo` (threepipe `EntityComponentPlugin.ts:429`). | `packages/editor/src/App.tsx:127-141` imports every script file from the manifest and calls `registerScripts` into the running viewer (`:134`). `packages/engine/src/runtime/createGame.ts:196-200` loops `config.scripts` only. `AGENTS.md:139` states the rule but the editor does not enforce it. | Make the editor register only `blitz.scripts`, and list unlisted script files with a warning; or make both sides discover scripts the same way. |
| G2 | major | editor + docs | `.blitz/console.log` receives `console.error` (during play) and uncaught errors only. `console.warn` never arrives. The file does not exist until the first error, so "no file" cannot be told from "not working". | Transcript 218 and 260: nine `[console.warn] [FpsDrill] pointer lock refused: WrongDocumentError ...` in the agent's Playwright output; transcript 232/233: `cat .blitz/console.log` printed nothing; transcript 209: `ls .blitz` gives `dev.json journal.jsonl`. My probe: a `console.error` line landed in the file, a `console.warn` line did not. | Play in the editor. Call `console.warn('x')` and `console.error('y')` from a component. Read `.blitz/console.log`: only `y`. | `packages/editor/src/App.tsx:391-395` wraps `console.error` only; `:104-108` gates forwarding on `playActiveRef`; `:71-95` appends to the file. `AGENTS.md:30` calls the file the feedback loop; `AGENTS.md:323` still says "visible in browser dev tools". | Wrap `console.warn` too, create the file with a header line at play start, and document the level filter. |
| G3 | minor | blitz dev server | `GET /files/<missing>` returns 500 with an `ENOENT` body and the absolute project path. The editor triggers one on the first `console.error`, when it reads `.blitz/console.log` before the file exists. | My probe: `[500] http://127.0.0.1:4323/files/.blitz/console.log` and browser console `Failed to load resource: the server responded with a status of 500`. `curl /files/does-not-exist.js` gives `500 {"error":{"code":"ENOENT","message":"ENOENT: no such file or directory, lstat '/Users/minjunes/blitz-games/fps-drill/does-not-exist.js'"}}`. | `curl -H 'X-Blitz-Token: <token>' http://127.0.0.1:<port>/files/nope.js`. | `packages/blitz/src/server.ts:261-264` calls `safeProjectPath` without `allowMissing`; `:598-620` throws on lstat; `app.onError` `:321-326` maps it to 500. The 404 branch in `serveProjectFile` `:623-624` is unreachable. | Pass `allowMissing` for GET and DELETE, or map `ENOENT` to 404 in `onError`. Do not echo absolute paths. |
| G4 | minor (blocker under strict mode) | cloud + blitz publish | The runtime registry still maps `0.12.0` to the old build. Every publish warns and ships the installed bytes. With `REQUIRE_REGISTERED_RUNTIME=true` the release would be refused. The agent never saw the warning: its `tail -15` cut it. | Agent `publish.log`: `[blitz] Installed runtime 4c904890405da72f4e65ef80d6c2a3573d078eab4bd75455920557c1190485d2 differs from registered 0.12.0 runtime 4c2d97fb20764e474c764510f736a53ac1f5268d0909121b1e9cf393359728ec; publishing the installed runtime.` `GET /api/v1/runtimes/0.12.0` gives `4c2d97fb...` 2,915,976 B; gateway `_blitz/runtime.js` ETag is `4c904890...` 2,916,577 B. | `curl -s https://blitz-backend.blitzapp.workers.dev/api/v1/runtimes/0.12.0` and compare with `shasum -a 256 node_modules/@blitzdev/engine/dist/runtime.js`. | `packages/blitz/src/publish.ts:112-123` compares and warns, never registers. Registry keyed by version: `blitz-cloud/backend/src/routes/releases.ts:87-93`; `backend/wrangler.prod.jsonc:22` keeps strict mode off. `packages/blitz/src/indexHtml.ts:17` writes the version only into `meta name=blitz-runtime`. | Re-register `0.12.0` with `4c904890...` now. Then key the registry by hash, register on publish, and write the hash into the meta tag. |
| G5 | paper cut | blitz dev server + docs | `/api/*` and `/files/*` reject the token from the URL. They need the `X-Blitz-Token` header or the cookie the page sets. AGENTS.md does not mention the header or any read endpoint. | Transcript 137/138: `curl -s "http://127.0.0.1:4323/api/state?t=<REDACTED>"` gives `Missing or invalid Blitz token`. The agent then grepped `server.ts` and `DevServerSource.ts`. | `curl "$(jq -r .url .blitz/dev.json | sed 's/?t=/api\/state?t=/')"`. | `packages/blitz/src/server.ts:122-127` (`checkToken`) versus `:129-131` (`serveIndex` reads `?t=`). | Accept `?t=` on GET for `/api/*`, or document the header plus `/api/state`, `/api/files`, `/api/events` in AGENTS.md. |
| G6 | paper cut | blitz command + docs | `npx blitz pull` before the first publish prints a friendly message but still exits 1. `AGENTS.md:14` and `:903` tell the agent to pull before every publish. | Transcript 262: `blitz: There is nothing to pull before the first publish.` The agent's exit-code print was empty (its own zsh bug), so it did not notice. | Run `npx blitz pull` in a fresh project; `echo $?` prints 1. | `packages/blitz/src/commands.ts:215` throws; `packages/blitz/src/cli.ts:144-146` turns every thrown error into exit code 1. | Return exit 0 with the message when no deploy exists, or change the docs to "after the first publish". |
| G7 | minor | blitz publish + template | Publish excludes are opt-in. The template ships no `blitz.publish.exclude`, so a default project publishes `AGENTS.md` (39 KB), `samples/`, and `tools/`. This agent added the exclude by hand after reading `publish.ts`. The published `package.json` also carries the local `file:../../blitz-packs/...` specs. | `package.json` gained `"publish": {"exclude": ["tools/**","samples/**","AGENTS.md"]}` (transcript 199). `packages/template/template/package.json` has no `publish` block. Live `package.json` shows the `file:` paths. | `blitz init x && cd x && npm install && npx blitz publish`, then `curl -sI <live>/AGENTS.md`. | `packages/blitz/src/filesystem.ts:3-4` (`DEFAULT_EXCLUDES` without docs or samples); `packages/template/template/package.json`. | Add `AGENTS.md`, `samples/**`, `tools/**` to `DEFAULT_EXCLUDES`, or stamp the block into the template. Strip `devDependencies` from the published `package.json`. |
| G8 | minor | editor | `.blitz/state.json` says `playing` after the editor tab closes mid-play. There is no unload write and no liveness field. Two open tabs overwrite each other. | Transcript 233: `"playState": "playing"`, `"updatedAt": "2026-09-10T06:04:44.975Z"`, read at 06:06:15 after the agent's Playwright browser closed at 06:05:44 without Stop. | Play, close the tab, read `.blitz/state.json`. | `packages/editor/src/App.tsx:109-125` writes state only from `readProject`, `play`, and `stop`; no `pagehide` handler; no client id or heartbeat. | Write `stopped` on `pagehide`, add `clientId` and refresh `updatedAt` every few seconds so a reader can call an old file stale. |
| G9 | paper cut | docs | `blitz.viewer` in `package.json` spreads into `ThreeViewer` options, but AGENTS.md never mentions it. The agent found `tonemap` by grepping threepipe. It needed it to keep unlit colours exact. | Transcript 170/171 and 185/186 (grep for `tonemap` in `ThreeViewer.ts`); `package.json` `"viewer": {"msaa": true, "tonemap": false}`. | Search AGENTS.md for `viewer`: only the API section. | `packages/engine/src/runtime/createGame.ts:87-89` (`...config.viewer`); `packages/engine/src/runtime/projectFormat.ts:42-44` types `msaa` only. Related to F12. | Document `blitz.viewer` keys and the default look (tonemap on, no environment map, `PhysicalMaterial` dark). |
| G10 | paper cut | engine | A leftover debug log prints `true` on every game boot, live and in the editor. | My headless loads: `[log] true` on the live page and on editor Play. | Open any published game with the console open. | `packages/engine/src/plugins/cannon/CannonPhysicsPlugin.ts:163` (`console.log(this.enabled)`). | Delete the line. |
| G11 | minor | blitz dev server + editor | The file manifest and the editor's "Project files" panel list `.blitz/deploys.json` and `.blitz/dev.json`. `GET /files/.blitz/deploys.json` returns the raw secrets to any holder of the editor token. | `/api/files` lists `.blitz/deploys.json`; my check: `GET /files/.blitz/deploys.json` returned `200 464B`. The editor body text starts `PROJECT FILES | .blitz/deploy...`. | `curl -H 'X-Blitz-Token: <token>' http://127.0.0.1:<port>/files/.blitz/deploys.json`. | `packages/blitz/src/server.ts:567-571` (`isIncludedPath` allows `.blitz`), `:142` (`/api/files`). `/api/deploys` `:159-171` already redacts. | Deny reads of `.blitz/deploys.json` and `dev.json` on `/files/*` and hide them from the manifest. |
| G12 | paper cut | blitz publish | The game description never reaches the backend. `package.json` `description` is ignored and publish sends no `metadata`. | `GET /api/v1/games/fps-drill` gives `"description": null` although `package.json` has a description. | Publish any project with a `description`; read the game record. | `packages/blitz/src/publish.ts:152-155` sends `message` and `base_release` only; `docs/publish-api.md` documents `metadata.description`. | Send `packageJson.description` as `metadata.description`. |
| G13 | minor | docs + command | No `blitz check` and no headless verification recipe. The agent wrote a 30-check Playwright driver from its own memory notes. Nothing validates script paths, generator module paths, or scene component types before publish. | `blitz --help` lists init, dev, publish, pull, status, claim, bake, journal, open, sources, upgrade. The only validation is `validateSceneSource` (JSON with an `asset` key) inside `upgrade`. | Point `blitz.scripts` at a missing file and publish: success, broken live. | `packages/blitz/src/cli.ts:18-34`; `packages/engine/src/runtime/projectFormat.ts:82-88`. | Add `blitz check`: resolve and import every script and generator module, verify scene component types are registered, report to `.blitz/console.log`. Add a headless recipe to AGENTS.md. |
| G14 | paper cut | blitz dev | Stale dev servers pile up. Three `blitz dev` processes listen on 4321, 4322, 4323 from three sessions. Free-port selection hides them. | `lsof`: pids 50909 (fps-practice, 5.5 h), 83366 (fps-shooter, 3.3 h), 27082 (fps-drill, this session). | Start `blitz dev` twice in two projects and look for a listing command: none. | `packages/blitz/src/server.ts:329-379` picks the next free port; `.blitz/dev.json` stores the pid but nothing reads it. | Let `blitz status` print the running server (pid, port, age) and let `blitz dev` warn when `.blitz/dev.json` points at a live process. |
| G15 | paper cut | docs | `docs/agents.md` (121 lines, served by the backend) and the template `AGENTS.md` (918 lines) have diverged; the cloud copy differs again. | `diff docs/agents.md packages/template/template/AGENTS.md` differs from line 1. | Compare the two files. | Two hand-maintained copies. | Generate one from the other in the release script. |

### Agent-side mistakes (not platform)

- Streak bonus applied on the first hit (`hit=165` instead of 150, transcript 218). The agent's own test caught it and it fixed `DrillManager.script.js:66-68`.
- Reload detector compared the stable component uuid (`fd-comp-drill-manager`) and waited 60 s for a change that cannot happen (transcript 218 and 257). Cost one full rerun.
- `EXIT=${PIPESTATUS[0]}` prints empty under zsh (`pipestatus` is lowercase). Exit codes of `npm install`, `blitz pull`, and the reruns were never seen. Same mistake as run 1.
- `tail -15` on the publish log hid the runtime registry warning (G4).
- Wrong memory note written at the end: "`blitz.name` in package.json is ignored by the CLI". `packages/blitz/src/publish.ts:210-217` honours it. The agent grepped for the literal `blitz.name` and missed the property access.
- Comment in `PlayerRig.script.js:37-38` says the Blitz editor page refuses pointer lock. Headless Chrome refuses it; the agent never tested the editor headed.
- One SSE probe put the editor session token on a curl command line (transcript 244). It stayed inside the agent's own shell, but the transcript now carries it.

## 2. F1 to F17 status

| id | status | evidence |
|---|---|---|
| F1 | FIXED | Gateway `_blitz/runtime.js` ETag `4c904890...` equals the installed `node_modules/@blitzdev/engine/dist/runtime.js` and the tarball. `Generator` ran live: transcript 278 `range generated by the published runtime :: 57 generated objects`. Residual: registry stale (G4), meta tag version-only. |
| F2 | FIXED | Every command ran with `file:` pins: `dev`, `pull`, `publish --help`, `publish`, `status`. `packages/blitz/src/version-pin.ts:42-47` resolves the installed version for non-exact specs. |
| F3 | FIXED | Transcript 252: `+1s {"status":"scripts/TargetField.script.js reloaded",...}`; transcript 260: `PASS script change restarts play into the menu`, `PASS exactly one HUD after reload :: hud count=1`, `PASS one viewmodel after reload`. Mechanism: `App.tsx:352-356` restarts through `play()`, `fileRevisions` add `?v=<sha256>` (`App.tsx:187-192`, `createGame.ts:233-239`), `scripts.ts:32-37` replaces the registered class. |
| F4 | FIXED | Live import map: `threepipe`, `three`, `uiconfig.js`, `ts-browser-helpers`, `@blitzdev/engine`, all to `./_blitz/runtime.js`. No esm.sh, no `file:`. `importMap.ts:59-63` skips `@blitzdev/*` and non-semver specs; `:30-37` reads `dependencies` only. |
| F5 | FIXED | One `dependencyImportMap` for both sides: `server.ts:584-587` (`/editor-runtime.js`) and `indexHtml.ts:11` (`./_blitz/runtime.js`). `/api/import-map` and the live map have identical keys. Extra dependencies would go to esm.sh on both sides; not exercised. |
| F6 | FIXED | Zero `Multiple instances of Three.js` in three headless loads (editor load, editor Play and Stop, live). `editor-runtime.js` (34,573 B) is a second Vite entry (`vite.config.ts:11-19`) that re-exports the engine graph (`editorRuntime.ts`); the injected import map routes project modules to it. Not exercised by the agent: its console filter matched `/FpsDrill|blitz/i` only. |
| F7 | PARTLY FIXED | Overwrite guard by code: `publish.ts:186-191` keeps locally modified files unless `--force`. Not exercised. Before the first publish `pull` still exits 1 (G6). |
| F8 | FIXED by code | `publish.ts:88-92` reads the live name when `--name` is absent. Not exercised: one publish only. |
| F9 | PARTLY FIXED | `console.error` during play and uncaught errors now land in `.blitz/console.log` (`App.tsx:71-108`, `:391-395`). `console.warn` does not; the file is absent until the first error (G2, G3). |
| F10 | FIXED | Transcript 67: `Blitz editor: http://127.0.0.1:4323/?t=<REDACTED>` with 4321 and 4322 busy and no `--port`. `server.ts:329-379` tries 20 ports. |
| F11 | ADDRESSED (docs) | `AGENTS.md:278` states the focus rule. Headless still refuses the lock (transcript 218 `pointer lock denied=true`); the agent built a cursor fallback and verified the lock headed: transcript 291 `{"locked":true,"denied":false,...}`, `yaw 0.176 -> -15.807`. Editor page headed: NOT EXERCISED. |
| F12 | STILL PRESENT (docs) | No lighting, tonemap, or environment guidance in AGENTS.md. The agent avoided it with `UnlitMaterial` everywhere and `tonemap: false`, both from its memory notes (G9). |
| F13 | NOT REPRODUCED | My editor probe: 50 `update` calls in 50 animation frames over 2 s (ratio 1.00), `deltaTime` median 33.4 ms, max 50.1 ms. Play waits for project load (`App.tsx:180-181`). The agent kept the wall-clock `lib/clock.js`, so its game never reads `deltaTime`. |
| F14 | FIXED | `indexHtml.ts:18` writes `<link rel="icon" href="./icon.svg">`; `icon.svg` returns 200; zero 4xx responses in the agent's live run (transcript 278 `badResponses: []`) and in mine. Gateway root `/favicon.ico` is still 404 but nothing requests it. |
| F15 | NOT REPRODUCED | Zero `texImage3D` warnings in editor load, editor Play and Stop, and live load. |
| F16 | PARTLY FIXED | `package-lock.json` is now default-excluded and returns 404 live. `AGENTS.md`, `samples/`, `tools/` return 404 only because the agent opted in (G7). |
| F17 | FIXED for the race | `play()` awaits `projectReadyRef` (`App.tsx:180-181`) and `readProject` writes `stopped` first (`:163-164`). My probe: `stopped`, then `playing`, then `stopped`. New stale-on-close variant (G8). |

Verification of the agent's five notes: 1 (editor refuses pointer lock) is headless, not the editor; 2 (blocked localStorage) is game-side; 3 (`Spin` listed) is G1; 4 (streak bug) is its own; 5 (no secrets printed) holds for the report, with the caveat above.

## 3. Timeline

- 05:41Z Reads three memory notes (engine gotchas, project, Playwright recipe). Lists `blitz-games/`, finds `fps-practice` and `fps-shooter`.
- 05:41Z `init fps-drill` exits 0. Install: `added 75 packages ... in 10s`; all four packages land in `devDependencies`.
- 05:42Z `npx blitz dev --no-open` starts on 4323. Reads AGENTS.md, then `fps-practice/ShootingRange.script.js` (855 lines) and every `fps-shooter` script, lib, generator, and the redacted `deploys.json`.
- 05:42Z to 05:53Z Reads engine, editor, server, and threepipe sources (14 source reads). 05:45Z `curl /api/state?t=...` gives `Missing or invalid Blitz token` (G5).
- 05:53Z to 06:02Z Writes 6 libs, 1 generator, 3 components, the scene tool, `main.js`, `package.json` (2,550 lines). `node --check` passes on all 12 files. Scene written by tool; journal records the external write.
- 06:04Z to 06:05Z Editor run 1: 23 of 24 pass; `precision ... hit=165` (own bug); reload wait times out after 60 s (own detector); nine pointer-lock warnings; `.blitz/console.log` absent (G2).
- 06:06Z `state.json` still `playing` after the browser closed (G8). SSE probe shows change events.
- 06:07Z Reload probe: `reloaded` after 1 s (F3).
- 06:08Z to 06:09Z Editor run 2: 30 of 30, empty error list. `pull` prints `There is nothing to pull before the first publish.` (G6).
- 06:09Z Publish: 17 files, 16 uploads, release `25d4afad...` in about 5 s. Registry warning hidden by `tail` (G4).
- 06:10Z Live run: 8 of 8, zero errors, zero failed requests; excluded files 404.
- 06:11Z Headed lock test passes. 06:12Z Memory notes written, report delivered.

## 4. What worked

- `init`, tarball install (10 s), and `dev` on a free port with the URL in `.blitz/dev.json` (mode 600).
- Watcher and SSE: a change event within the 150 ms debounce; script hot reload restarts the game in about 1 s with one HUD and one viewmodel.
- The `Generator` component ran in the editor and live; the generator module imported `../lib/textures.js` with a relative path.
- `.blitz/journal.jsonl` recorded the tool's scene write as `external` with the four node additions.
- Publish: create, hash, upload only missing blobs, release, `deploys.json` written; `--slug`, `--name`, `--message` honoured; `blitz status` prints no secrets.
- Gateway: correct MIME types (`text/javascript`, `model/gltf+json`, `image/svg+xml`, `application/json`), strong ETags, identity encoding, 404 for every excluded or dotfile path.
- Editor: Play waits for load, `state.json` transitions are correct in my probe, Stop leaves zero HUDs and no pointer lock.
- The agent's Playwright suites (30 editor checks, 8 live checks) and the headed pointer-lock test.

## 5. Live game check

URL: `https://blitz-game-gateway.blitzapp.workers.dev/fps-drill/`. Game `6f05ff7b-5f3e-4d13-908d-b1066df7d33c`, one release `25d4afad...` (`initial release`, 17 files, `bytes_used` 3,006,364), unclaimed, expires `2026-09-10 18:09:25` UTC, `description` null.

- `index.html`: all paths relative (`./_blitz/runtime.js`, `./icon.svg`, `base: new URL('./', location.href)`). Import map has five keys, all to `./_blitz/runtime.js`; no `file:` and no esm.sh.
- `_blitz/runtime.js`: `200`, `text/javascript; charset=utf-8`, 2,916,577 B, ETag `"4c904890..."`, equal to the installed runtime.
- Headless boot (swiftshader): `window.viewer` present, title `FPS Drill`, 62 objects under `modelRoot`, 57 generated, top level `GameRoot, Range, Player, Targets`, menu visible in 14.8 s (5.4 s in the agent's run). Console: one `true` (G10), the threepipe banner, GPU stall warnings from swiftshader. Zero errors, zero 4xx.
- Excluded: `AGENTS.md`, `tools/write-scene.mjs`, `samples/Spin.script.js`, `package-lock.json`, `.blitz/deploys.json`, `.blitz/dev.json`, `.gitignore`, `node_modules/...` all `404`. Published: `package.json`, `assets.json`, `assets/main.scene.gltf`, `main.js`, `icon.svg`, 6 libs, 3 scripts, 1 generator.
- Manifest versus folder: 17 of 17 entries match the local hashes (`_blitz/runtime.js` against `node_modules/@blitzdev/engine/dist/runtime.js`). `.blitz/deploys.json` `last_release_hash` equals the active release.

## 6. Project check

- Representation rules: the range is a `Generator` component on the `Range` node (`generators/range.js`, 57 children, tagged `blitzGenerated` and `excludeFromExport`). Three components are listed in `blitz.scripts`. Stable ids (`fd-node-*`, `fd-comp-*`). Scene written only by `tools/write-scene.mjs`, which merges by name and keeps foreign extras.
- Stopped mode: the editor shows no 3D view when stopped (my load: `hasViewer: false`), so stopped-mode content is moot in this editor. All three components create everything in `start()` and remove it in `stop()`: HUD, audio, background colour, camera state, gun, effects, decals, listeners, pointer lock, target geometry and materials. Verified by the agent (`stop removes the HUD`, `stop leaves no pointer lock`) and by my probe.
- Scene file: text glTF, pretty JSON, four nodes, no buffers, so no `.bin` exists (`assets/main.scene.bin` is 404 live). It never passed through the editor serializer; `asset.generator` is `Blitz` (tool) not `Blitz 0.12.0`.
- Validation: no `blitz check` exists (G13). The agent used `node --check` and Playwright. Its own streak bug shows the platform offers no unit-level harness; its 60 s reload detector failure shows component ids are not documented as stable scene ids.
- Time base: every component uses the wall-clock `FrameClock` from `lib/clock.js` and ignores `e.deltaTime`, a carry-over from run 1 (F13).

## 7. Agent quality

- Not a fresh user. First action: read memory notes from run 1. Second: read both earlier projects. Five of six `lib/` files derive from `fps-shooter` (`clock.js` differs by 6 lines, `audio.js` 40, `fx.js` 52), and `tools/write-scene.mjs` is the same tool with new node specs. The run measures an experienced agent, and the F-list was pre-loaded through the notes.
- Good: read AGENTS.md in full; followed the scene, generator, script-listing, exclusion, and cleanup rules; exposed `window.fpsDrill` test handles from `main.js`; tested before publishing and again live; verified pointer lock in a headed window; kept the dev server alive; reported concisely with edge cases; filtered publish output for `token` and `secret`; redacted `deploys.json` when reading the old project.
- Weak: no exit-code visibility (zsh `PIPESTATUS`), warnings lost to `tail`, one wrong memory note, one stale comment, 12 of 31 minutes spent grepping sources for things AGENTS.md does not say (`blitz.viewer`, `X-Blitz-Token`, reload mechanics, `?v=` cache busting, editor test selectors, `tonemap`).
- The final report was accurate on all five points.

## 8. State left behind

- Running: pid 27057 and child 27082 `blitz dev --no-open` on 4323 for `fps-drill` (kept, as asked); pid 50909 on 4321 (`fps-practice`) and pid 83366 on 4322 (`fps-shooter`) from earlier sessions.
- Scratchpad: `editor-test.mjs`, `live-test.mjs`, `lock-test.mjs`, `reload-probe.mjs`, `publish.log`, `sse.log`, `blitz-dev.log` (contains the tokenized URL), `shots/` (2.8 MB). Memory notes `project_fps_drill.md` and an update to `reference_blitzdev_engine_gotchas.md` (one wrong line, see above).
- This review added under `.blitz/`: `console.log` with one `[review-probe]` line, and a rewritten `state.json` (`stopped`), both through the editor's normal write path. Review scratch files live in `scratchpad/review2/`.

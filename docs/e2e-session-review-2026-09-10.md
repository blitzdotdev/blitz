# E2E session review, 2026-09-10

Scope: audit of agent session `a7f0062a3a757de8b` (02:53Z to 03:49Z, 47 Bash, 16 Write, 11 Read calls). The agent built `Target Rush` in `/Users/minjunes/blitz-games/fps-shooter` from the 0.12.0 tarballs, published it, and reported ten problems. This review verifies each one, adds unreported findings, and records the state left behind. Nothing was fixed or killed. No token, secret, or editor URL token appears in this file.

Sources: the transcript, the project folder, the tarball sources under the project's `node_modules/@blitzdev/*/src` (the code that ran), the monorepo at `main` (`b71b68e`), the live game loaded headlessly with Playwright, and `GET /api/v1/runtimes`.

## 1. Findings, ranked by severity

Line numbers point at the tarball sources under `node_modules/@blitzdev/<pkg>/src` unless the path starts with `packages/` (monorepo `main`).

| id | severity | area | summary | exact error or symptom | repro | suspected file:line |
|---|---|---|---|---|---|---|
| F1 | blocker | cloud + blitz publish | Publish attaches the server-registered runtime for the version string, not the local bundle. The registered 0.12.0 blob was an older build without `Generator`. The live game had no arena. | Live console: `EntityComponentPlugin: unknown component type Generator`. Gateway `_blitz/runtime.js` is 2,885,697 B, etag `4afa7a94...`, zero hits for `blitz-generator`. Local `engine/dist/runtime.js` is 2,915,976 B, sha `4c2d97fb...`, has `Generator`. | 1. `curl -sI .../fps-shooter/_blitz/runtime.js` and compare the etag with `shasum -a 256 node_modules/@blitzdev/engine/dist/runtime.js`. 2. Publish a scene with a `Generator` node. 3. Open the live URL and read the console. | `blitz/src/publish.ts:101` (`api.getRuntime(version)`), `:111` (`buildManifest(projectEntries, runtime)`); no hash comparison. `blitz/src/indexHtml.ts:30` writes only the version into `meta name=blitz-runtime`, so pages cannot show which build they run. Now: `/api/v1/runtimes` lists sha `4c2d97fb...` (re-registered after the session), but the game's releases still pin the old blob, and `packages/engine/dist/runtime.js` has drifted again (sha `ff312e76...`, 2,916,333 B) under the same `0.12.0`. `packages/blitz/src/publish.ts:100-108` adds only a 404 message. |
| F2 | blocker (next run) | blitz command | The new version-pin rule rejects the `file:` spec that the documented tarball install writes into `devDependencies`. Every command except `--help` and `--version` fails. The rebuilt tarballs (20:53) carry the rule; the session's tarballs did not. | `blitz: Project pins @blitzdev/blitz file:../../blitz-packs/blitzdev-blitz-0.12.0.tgz. Run npm install, or npx @blitzdev/blitz@file:../../blitz-packs/blitzdev-blitz-0.12.0.tgz status` (exit 1). With a local binary of the same code it first prints `Blitz 0.12.0 does not match project pin file:...; delegating to .../node_modules/.bin/blitz`, then the same error. | Reproduced in a scratch project: `package.json` with `"@blitzdev/blitz": "file:../../blitz-packs/blitzdev-blitz-0.12.0.tgz"`, then `node /Users/minjunes/blitz/packages/blitz/dist/cli.js status`. With `"0.12.0"` the same command exits 0. Step 3 of `docs/testing-with-an-agent.md` (`npm install --install-links .../*.tgz`) produces the `file:` pin. | `packages/blitz/src/version-pin.ts:19-20` (any non-empty string is the pin), `:37`, `:49`; `packages/blitz/src/publish.ts:188-195` (`must be an exact x.y.z version`). Workaround exists: `BLITZ_IGNORE_VERSION_PIN=1` (`version-pin.ts:34`). |
| F3 | major | engine + editor | Script hot reload keeps the old component class. The editor restarts the game, but the running game still uses the old module until a page reload. | Editor status `scripts/FpsController.script.js reloaded` after 561 ms; the hint text stayed `CLICK THE ARENA TO CAPTURE THE MOUSE`; after a page reload it read `... (V2)`. | 1. Play in the editor. 2. Change a string in a `.script.js`. 3. Wait for `reloaded`. 4. Read the string in the running game. | Root cause has two parts. `engine/src/runtime/createGame.ts:188-193` imports each script by its bare URL (`importModule(scriptUrl.href)`, `:237-238` `import(specifier)`), so the browser module map returns the old instance. `engine/src/scripts.ts:33` then skips the fresh class because `hasComponentType` is already true. The editor's own import uses `?v=<sha256>` (`editor/src/App.tsx:103`, `DevServerSource.ts:115-118`) but cannot replace the type. Generators do get a revision query (`engine/src/plugins/GeneratorComponent.ts:123`). Contradicts `AGENTS.md:144` and `docs/testing-with-an-agent.md` ("Script saves by the agent hot-reload"). |
| F4 | major | blitz publish | The published import map contains esm.sh URLs built from raw `file:` specs. They 404 and leak the local folder layout. Not reported by the agent. | Live `index.html`: `"@blitzdev/engine":"https://esm.sh/@blitzdev/engine@file:../../blitz-packs/blitzdev-engine-0.12.0.tgz?external=..."`, same for `@blitzdev/editor` and `@blitzdev/template`. esm.sh answers `404` with `package '@blitzdev/blitz-packs' not found`. The published `package-lock.json` has 8 `blitz-packs` paths. | `curl -s https://blitz-game-gateway.blitzapp.workers.dev/fps-shooter/ \| grep importmap`. Any script with `from '@blitzdev/engine'` fails live. | `blitz/src/indexHtml.ts:18-22` uses `dependency.version` verbatim; `engine/src/runtime/projectFormat.ts:85-92` copies every `dependencies` entry. The install step put all four tarballs into `dependencies`, including the dev-only editor and template. |
| F5 | major | editor + blitz publish | Dev and published import maps differ. `@blitzdev/engine` resolves in the editor but not live. Declared project dependencies resolve live (esm.sh) but not in the editor. | Editor `index.html` import map: `threepipe, three, uiconfig.js, ts-browser-helpers, @blitzdev/engine` to `/_blitz/runtime.js`. Published map: `RUNTIME_SPECIFIERS` has no `@blitzdev/engine`. `dependencyImportMap` has no callers. | By code reading, not executed. Import `HtmlUiComponent` from `@blitzdev/engine` in a script: works in the editor, fails live. Declare `gsap` in `dependencies`: fails in the editor with an unresolved specifier, works live. | `blitz/src/indexHtml.ts:3`; `editor/dist/index.html` import map (`packages/editor/vite.config.ts`, `importMapPlugin`); `engine/src/importMap.ts:6` unused; `blitz/src/server.ts:107-110` serves the static editor page unchanged. `AGENTS.md:904` ("Bare imports must be declared in package.json") describes only the live side. |
| F6 | major | editor | The editor bundle ships its own copy of three.js, threepipe and the engine next to `/_blitz/runtime.js`. Two class copies live in one page. | Console after reload: `WARNING: Multiple instances of Three.js being imported.` `editor/dist/assets/index-DyuvHtyj.js` (2.4 MB) contains `Multiple instances of Three.js`, `animation loop: frame skip`, `EntityComponentPlugin must be added before project scripts`. | `grep -c "Multiple instances of Three.js" node_modules/@blitzdev/editor/dist/assets/index-*.js` gives 1. Open the editor with any script and read the console. | `packages/editor/vite.config.ts` (`importMapPlugin` injects the map but the build still bundles the packages). Project scripts extend `runtime.js` classes; the viewer and `EntityComponentPlugin` come from the bundle. `instanceof` across copies fails. The live page has one copy. |
| F7 | major | blitz command + docs | `blitz pull` fails before the first publish and overwrites local edits afterwards. AGENTS.md requires it before every publish. | `blitz: No deploy exists yet. Run blitz publish first.` | Run `npx blitz pull` in a fresh project. After a publish, edit a file locally and run `pull`: the file is replaced. | `blitz/src/publish.ts:154-161` downloads every release file whose hash differs and writes it, with no diff and no prompt. `AGENTS.md:14` and `:898` ("Run npx blitz pull before every update and resolve any local and remote difference"). |
| F8 | major | blitz publish | A publish without `--name` regenerates `index.html` with the package name as title. The game name given at creation is lost. | Live test after the second publish: `page title (fps-shooter)`. After `--name "Target Rush"`: `Target Rush`. | Publish with `--name "X"`, then publish again with only `--message`. Read `<title>`. | `blitz/src/publish.ts:103` (`name \|\| packageJson.name \|\| slug`) on every publish; the deploy entry stores no name. `AGENTS.md:898` says later publishes reuse the saved entry. |
| F9 | major | editor + docs | `.blitz/console.log` receives only uncaught errors and unhandled rejections. `console.error` and `console.warn` never reach an agent without a browser. The file is never created when nothing is thrown. | The session produced six browser warnings and one live `console.error`; `.blitz/console.log` does not exist. `fps-practice` got a log only because its rejection was unhandled. | Add `console.error('x')` in `start()`. Play. Check `.blitz/console.log`. | `editor/src/App.tsx:58-79` (`appendConsoleError` via `reportError`), `:338-341` (`error`, `unhandledrejection` only). `AGENTS.md:26`, `:87` call it the feedback loop; `AGENTS.md:318` points to "browser dev tools". |
| F10 | minor | blitz command | Default port busy: the command exits with a raw socket error, no `--port` hint, no stale-server detection from `.blitz/dev.json`. | `blitz: listen EADDRINUSE: address already in use 127.0.0.1:4321` (exit 1, reproduced with `packages/blitz/dist/cli.js`). | Start `blitz dev` twice, or with `fps-practice` (pid 50909) still listening on 4321. | `blitz/src/server.ts:342-348` (`server.once('error', reject)`, default `4321`); `blitz/src/cli.ts:121-123`. |
| F11 | minor | browser behavior + docs | Pointer lock is refused when the Chrome window is not focused. Focused headed Chrome works. Not a platform bug. | `[TargetRush] pointer lock refused: WrongDocumentError The root document of this element is not valid for pointer lock.` Headless Playwright reproduces it even with `document.hasFocus()` true. Agent's headed runs: `locked=true`. | Load the live URL in headless Chromium, click the canvas. | No platform file. Docs gap: `AGENTS.md` has no pointer-lock or FPS input guidance and no headless verification recipe; `docs/testing-with-an-agent.md` is not part of the project. |
| F12 | minor | game-side + docs | Scene too dark. The agent's own materials and light intensities; fixed at 03:36Z. | Screenshots `shot-03-playing.png`, `shot-04-final.png` (dark arena). | Author a scene with `PhysicalMaterial` and one directional light, no environment map. | Game files `generators/arena.js`. `AGENTS.md` has no lighting, tonemapping or environment guidance beyond `viewer.setEnvironmentMap` (`:378`). |
| F13 | minor (unreproduced) | engine or editor | Game clock ran about 3.5x fast in one run. Three later runs measured 121 updates per second with 8.3 ms deltas. | First harness run: `timeLeft` 57.23 about 0.9 s after the start click; after `R` restart 57.09 after 0.9 s. Conditions: Play clicked while the editor still showed `Loading project…`, window unfocused, pointer lock denied. | Not reproduced. Needs an instrumented run that counts `update` calls per animation frame and logs `e.deltaTime`, with Play pressed during project load. | Static reading excludes duplicate dispatch (`threepipe/src/plugins/extras/EntityComponentPlugin.ts:73,76`, map keyed by object plus component uuid), `maxFramePerLoop` (`threepipe/src/viewer/ThreeViewer.ts:387` is 1, loop at `:812`), and the game's `deltaTime ?? 16` fallback (about 1.9x at 120 Hz). Open candidates: `editor/src/App.tsx:147-165` `play()` racing `readProject` (`:111-134`); `threepipe/src/rendering/RenderManager.ts:101,148-150` (`_lastTime` starts at 0). The agent kept a wall-clock workaround (`lib/clock.js`). |
| F14 | minor | cloud + blitz publish | favicon 404 and no icon link in the generated page, although the template ships `icon.svg`. | `Failed to load resource: the server responded with a status of 404 () [https://blitz-game-gateway.blitzapp.workers.dev/favicon.ico]`. `curl` gives 404 for `/favicon.ico` and 200 for `/fps-shooter/icon.svg`. | Open any live game. | `blitz/src/indexHtml.ts:25-43` (no `<link rel="icon">`); gateway root has no favicon route. |
| F15 | minor | engine (threepipe) | 3D LUT texture upload warning after reload. | `WebGL: INVALID_OPERATION: texImage3D: FLIP_Y or PREMULTIPLY_ALPHA isn't allowed for uploading 3D textures` (twice). | Reload the editor with the game loaded. | Not pinned to a line. `engine/dist/runtime.js` contains `Data3DTexture` (10 hits) and `LUTCubeLoader`; a 3D texture is uploaded with `flipY` set. |
| F16 | minor | blitz publish | Publish ships every file outside `.blitz`, `.git`, `node_modules`, `dist`. Docs, lockfile and tooling go public. | Live 200 for `AGENTS.md` (911 lines), `package-lock.json`, `tools/write-scene.mjs`, `samples/Spin.script.js`. | `curl -sI .../fps-shooter/AGENTS.md`. | `blitz/src/filesystem.ts:3` (`EXCLUDED_DIRECTORIES`); documented at `AGENTS.md:910`. Compounds F4. |
| F17 | minor (by code reading) | editor | `.blitz/state.json` can report `stopped` while playing, when Play is pressed during project load. | Not observed; the session's final `state.json` is consistent. | Click Play while the status is `Loading project…`; read `playState`. | `editor/src/App.tsx:132-133` (`writeState(false)` at the end of `readProject`) versus `:160-162` (`writeState(true)` in `play`). |

Verification of the agent's ten items: 1 = F10 (avoided, then reproduced here), 2 = tooling, not platform (extension not connected; see section 2), 3 = F11, 4 = F12, 5 = F13, 6 = F1, 7 = F3, 8 = F7, 9 = F8, 10 = F14 + F6 + F15. All ten statements were accurate. Items 1 and 5 were not reproduced by the agent; 1 is reproduced here, 5 is not.

Problems the agent did not report: F2, F4, F5, F6 (cause), F9, F16, F17.

### Timeline of problems, with the exact text

- 02:58Z `lsof` shows `node 50909` on `127.0.0.1:4321` (`fps-practice`, started 17:42 local). Agent starts on 4322 instead. Never hit the error (F10).
- 03:19Z `node -e require("./assets/main.scene.gltf")` fails with `SyntaxError: Unexpected token ':'`. Agent mistake (require of JSON with a `.gltf` suffix), not platform.
- 03:19Z `mcp__claude-in-chrome__tabs_context_mcp`: `Browser extension is not connected...`. Agent writes `cdp.mjs`.
- 03:23Z first editor run, 10 of 16 checks: `WrongDocumentError The root document of this element is not valid for pointer lock.` (3x), `timeLeft` 57.23 after 0.9 s (F13), click did not fire.
- 03:34Z to 03:38Z timing probes: 121 frames and 121 updates per second, `maxDelta` 9.4 ms; focused run passes 20 of 20.
- 03:36Z brightens materials and lights (F12).
- 03:40Z hot reload test: `reloaded` after 561 ms, hint `STALE (browser module cache)`, `FRESH` after page reload (F3). Console: `Multiple instances of Three.js` (2x), `texImage3D` (2x).
- 03:42Z `npx blitz pull`: `blitz: No deploy exists yet. Run blitz publish first.` (F7). First publish, 14 files, release `32a75996...`.
- 03:43Z live test 3 of 6: `EntityComponentPlugin: unknown component type Generator` (F1), two `404` (favicon, F14), `arenaChildren: 0`.
- 03:46Z `ArenaBuilder.script.js` replaces the Generator; second publish `738ddc52...`; live 6 of 6 but `page title (fps-shooter)` (F8).
- 03:48Z third publish with `--name`, `0f2ddc94...`; final live 6 of 6.

Notes on silent failures: the agent's `exit=${PIPESTATUS[0]}` printed empty in every call (zsh uses `pipestatus`), so it never saw an exit code for `npm install`, `publish` or its harness. Publish and pull output was `tail`ed. Retries: five editor harness runs, three harness edits to resume after focus-loss pauses, three publishes, three live runs. The focus problem cost about 25 minutes.

## 2. Workarounds a normal user would not have done

1. Wrote a Chrome DevTools Protocol driver (`cdp.mjs`, 6.7 KB) and five harness scripts, and drove a separate Chrome profile, because the Chrome extension was not connected.
2. Checked `lsof` for port 4321 and started the editor on `--port 4322`.
3. Replaced the `Generator` component with a hand-written `ArenaBuilder` component that calls the same generator module, to dodge the hosted runtime (F1). The live game still runs the old runtime blob.
4. Wrote `lib/clock.js` and removed the engine `deltaTime` from every component (F13).
5. Skipped `blitz pull` before the later publishes on purpose (F7).
6. Republished with `--name "Target Rush"` to restore the title (F8).
7. Built a cursor-aim fallback with Q, E, arrows and screen-edge turning for refused pointer lock (F11).
8. Reloaded the editor page to pick up script edits (F3).
9. Grepped `threepipe/src` for constructor signatures, `shadowMap`, tonemapping, `maxFramePerLoop` and the frame loop, because AGENTS.md does not cover lighting or timing internals.

## 3. What worked well

- `blitz init` and the tarball install (73 packages, 2 minutes) produced a runnable project. `blitz dev --no-open --port 4322` printed the URL and wrote `.blitz/dev.json` (mode 600).
- The file watcher picked up scripts written without the editor open, and journaled both external scene writes (`.blitz/journal.jsonl`, two `external` entries). `blitz journal -n 5` and `blitz status` worked and printed no secrets.
- The editor registered four custom components, ran the Generator, and wrote `.blitz/state.json`. The final editor run passed 20 of 20 checks with an empty console.
- Publish created the game, uploaded only missing blobs, and cut three releases in under a minute each. `--slug`, `--name`, `--message` behaved as documented.
- The live game works today: my headless load found `window.viewer`, 45 objects, `Arena(28)`, title `Target Rush`, `blitz-runtime 0.12.0`, no errors except the pointer-lock warning.
- The agent's report was accurate on all ten points and it kept the secrets out of the transcript.

## 4. State left behind

Running processes:

- pid 83366 `node /Users/minjunes/blitz-games/fps-shooter/node_modules/.bin/blitz dev --no-open --port 4322` (parent 83338 `npm exec blitz dev --no-open --port 4322`, shell 83335), started 20:01:34 local. This agent's editor. The prompt asked to keep it running. URL `http://127.0.0.1:4322/?t=<token>`.
- pid 50909 `node /Users/minjunes/blitz-games/fps-practice/node_modules/.bin/blitz dev --no-open` (parent 50884 `npm exec blitz dev --no-open`), started 17:42:17 local on port 4321. From an earlier session, not this agent.
- pid 36471 `Google Chrome --headless=new ... --remote-debugging-port=9223 --user-data-dir=/private/tmp/claude-501/-Users-minjunes-blitz/6698d49e-b068-44a8-866f-bb3d6f559506/scratchpad/harness/chrome-tick http://127.0.0.1:8731/test/harness.html`, started 17:10 local. Another session, not this agent.
- pids 14015 and 18258 `wrangler dev ... --port=8686` (teenybase), unrelated.
- The agent's test Chrome (port 9333) is gone; port 9333 is free.

Temp files and dirs:

- `/private/tmp/claude-501/-Users-minjunes-blitz/278f5f97-7e16-4722-b1bc-c061014966db/scratchpad/chrome-profile/` (115 MB).
- Same folder: `cdp.mjs`, `test-editor.mjs`, `debug-editor.mjs`, `probe-timing.mjs`, `test-reload.mjs`, `test-live.mjs`, `dev.log`, `live-runtime.js` (2.9 MB), 12 PNG screenshots (about 5 MB).
- `/tmp/blitz-npm-cache` (1.0 GB), created by the install command in the prompt.
- From this review: `review-live.mjs`, `review-live.png`, `gw-runtime.js`, `gw-headers.txt`, `pinrepro/` in the same scratchpad.

Project and cloud state:

- `/Users/minjunes/blitz-games/fps-shooter`: 4 scripts, `generators/arena.js`, 5 `lib` modules, `tools/write-scene.mjs`, generated `index.html`, `package.json` with `file:` specs for all four packages. `.blitz/`: `deploys.json`, `dev.json`, `journal.jsonl` (2 entries), `state.json` (`stopped`, 03:48:25Z). No `console.log`.
- Cloud: game `c5915c52-42fe-4309-af17-6c7c305283b8`, slug `fps-shooter`, three releases, last `0f2ddc94...`, unclaimed, expires `2026-09-10 15:42:48` UTC. The releases pin runtime blob `4afa7a94...` (no `Generator`).
